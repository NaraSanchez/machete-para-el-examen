Un árbol se puede
	definir como una
	estructura jerárquica
	y en forma no lineal,
	aplicada sobre una
	colección de
	elementos u objetos
	llamados nodos.
	(Cairó & Guardati,
	 2006).
	
	arboles binarios distintos. Dos árboles binarios son distintos cuando sus estructuras son
	diferentes.
	Árboles binarios similares. Dos árboles binarios son similares cuando sus estructuras
	son idénticas, pero la información que contienen sus nodos difiere entre sí.
	Árboles binarios equivalentes. Los árboles binarios equivalentes se definen como aquellos
	que son similares y además los nodos contienen la misma información.
	
	Recorrido INORDEN. Este recorrido se realiza así: primero recorre el
	subárbol izquierdo, segundo visita la raíz y por último, va al subárbol
	derecho.
	En síntesis: hijo izquierdo — raíz — hijo derecho
	
	Recorrido PREORDEN. Este recorrido se realiza así: primero visita la raíz;
segundo recorre el subárbol izquierdo y por último va a subárbol derecho.
	En síntesis: raíz — hijo izquierdo — hijo derecho
	
	Recorrido POSORDEN. Primero recorre el subárbol izquierdo; segundo,
		recorre el subárbol derecho y por último, visita la raíz.
		En síntesis: hijo izquierdo– hijo derecho — raíz
